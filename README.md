# RenderSpace

Monorepo for RenderSpace prototype.

- `services/web`: Cloud Run web app (Flask + Three.js landing page)
- `services/worker`: GPU worker service (FastAPI) for TRELLIS reconstruction (stub for now)
- `libs`: shared Python modules (future)
- `infra`: infrastructure docs and scripts (future)
